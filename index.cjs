/*
	This file attempts to allow backwards compatability 
	for anyone previously self-hosting the bot.
	
	If you're not one of those people, it's best to start the bot
	with `pnpm start` (or the pm2 ecosystem file) instead.
*/

import('./src/standalone.js')
